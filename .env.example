# Browser-visible values must use the VITE_ prefix. Keep real values out of git.
VITE_API_BASE_URL=http://localhost:8787/api/v1
APP_ORIGIN=http://localhost:5173
LOG_LEVEL=info
SENTRY_DSN=

# Local-only D1 database name; production binding is configured by the host.
D1_DATABASE_NAME=mealflow-local
